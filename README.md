# Automate Youtube Tasks


```bash
PROJECT_DIR="projects"
GITHUB_REPO="https://github.com/aimldl/automate-youtube-tasks.git"

cd && mkdir -p ${PROJECT_DIR} 
cd ${PROJECT_DIR}
git clone ${GITHUB_REPO}
```
